import { Typography, Box } from '@mui/material';

const Inicio = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Bienvenido al sistema
      </Typography>
      <Typography variant="body1">
        Usa el menú lateral para acceder a los módulos de gestión.
      </Typography>
    </Box>
  );
};

export default Inicio;
